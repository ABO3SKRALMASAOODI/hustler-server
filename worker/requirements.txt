psycopg2-binary==2.9.10
boto3==1.35.99
pydantic==2.9.2
openai==1.59.9
faster-whisper==1.1.1
scenedetect==0.6.4
opencv-python-headless==4.10.0.84
numpy<2
pillow==10.4.0
requests==2.32.3
# Media extraction from page links (YouTube, TikTok, Vimeo, SoundCloud) for
# worker/url_media.py. NEEDS REGULAR BUMPING: sites change their players
# constantly and a months-old yt-dlp starts failing on the biggest ones first.
# A failure here is honest (the tool reports the extractor's own reason) but it
# is still a broken feature, so treat a stale pin as a bug, not as drift.
yt-dlp==2026.7.4
# Headless-Chromium website capture for worker/webrecord.py (record_website
# tool). The BROWSER itself is baked by the Dockerfile's
# `playwright install --with-deps chromium` — keep that line and this pin in
# the same commit when bumping, or the runtime downloads a browser on a box
# with no disk to spare.
playwright==1.49.1
